<%= form_for(@passport) do |f| %>
  <% if @passport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passport.errors.count, "error") %> prohibited this passport from being saved:</h2>

      <ul>
      <% @passport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="field">
    <%= f.label :precio %><br>
    <%= f.number_field :precio %>
  </div>
  <div>
  <div>
 <%= f.label :route %>
<%= f.select :route_id, Route.all.collect {|c| [c.vias, c.id]} %>
</div>
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
