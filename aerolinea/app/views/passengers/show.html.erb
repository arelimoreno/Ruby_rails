<p id="notice"><%= notice %></p>

<div class="jumbotron", "panel panel-primary">
  <h1>Bienvenido <%= @passenger.nombre %></h1>
  <p>
  <strong>Apellido:</strong>
  <%= @passenger.apellido %><br>
</p>

</div>
<p><strong>Identificacion:</strong>
  <%= @passenger.identificacion %></p>

 <p> <strong>Edad:</strong>
  <%= @passenger.edad %> Años<br>
</p>

<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">Tequetes</div>
  <!-- Table -->
  <table class="table">
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Precio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @passenger.passports.each do |p| %>
      <tr>
        <td><%= p.tipo %></td>
        <td><%= p.precio %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>


<%= link_to 'Edit', edit_passenger_path(@passenger) %> |
<%= link_to 'Back', passengers_path %>
