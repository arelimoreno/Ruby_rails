<p id="notice"><%= notice %></p>
<div class="jumbotron", "panel panel-primary">
<p>
  <strong>Nombre:</strong>
  <%= @pilot.nombre %>
</p>
<p>
  <strong>Apellido:</strong>
  <%= @pilot.apellido %>
</p>
<p>
  <strong>Identificacion:</strong>
  <%= @pilot.identificacion %>
</p>

<p>
  <strong>Cargo:</strong>
  <%= @pilot.cargo %>
</p>


<p><strong>avion:</strong>
<%= @pilot.airplane_id %>
</p>
</div>

<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">Rutas de vuelo</div>
  <!-- Table -->
  <table class="table">
  <thead>
    <tr>
      <th>Origen</th>
      <th>Destino</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pilot.routes.each do |route| %>
      <tr>
        <td><%= route.origen %></td>
        <td><%= route.destino %></td>
        <td><%= route.horario_salida %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div> 
</p>




<%= link_to 'Editar', edit_pilot_path(@pilot) %> |
<%= link_to 'Lista de pilotos', pilots_path %>
